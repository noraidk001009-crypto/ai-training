# Day 08

Build PDF: `pdflatex lesson.tex` or run `scripts/build-all.ps1`.


A:Row 2 has nothing in the command column (2,,18,10,...).
Row 3 stores player_hp as the word twenty instead of a number (3,fight,twenty,8,...)
Round 3 appears twice (rows 3 and 4).