# Day 06: Lists \& Inventory

## For Nora
1. Read **Nora-Day-06.pdf** (in this folder after build)
2. Practice with `demos/`
3. Submit work in `assignment/`

## Build PDF (parent)
```powershell
cd "day06"
pdflatex -interaction=nonstopmode lesson.tex
```
why does index start at 0?
because they count how far you are from the start. Usually we start at 0 so that's why index start at 0.

## What I tried
- I fixed the bug in `buggy_list.py` by my self
- I known the meaning of **append**, **len**, **index[0]**
- I wrote a code by myself

One line I can teach my friend
In code, length is the full name of **len**.
